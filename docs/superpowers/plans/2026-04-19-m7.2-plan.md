# M7.2 — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-04-19-m7.2-trees-design.md`
**Task:** #18 (in_progress)

## Task Breakdown

### T1 — `PlantRules` table + `PlantType` enum

- New `src/voxel/plant.c3`: `PlantType` enum, `MAX_PLANT_GEN` const, `PlantRules` struct, `PLANT_TABLE: PlantRules[PLANT_TYPE_COUNT]`.
- Port all 6 VQ variants verbatim (OAK_TRUNK/OAK_ROOTS/OAK2_TRUNK/OAK2_ROOTS/BARE_OAK_TRUNK/BARE_OAK_ROOTS). Use `PI` from `std::math` (or `core::deg_to_rad`).
- No callsites yet.
- **Commit:** `m7.2: PlantRules table + PlantType enum`

### T2 — `PlantNode` + `apply_rules` recursion

- New `src/world/plant_gen.c3`: `PlantNode` struct + `PlantNodeList` alias; `MAX_NODES_PER_TREE` = 1024.
- `plant_frand` / `plant_frand_range` helpers (wrap `voxel::voronoi_hash` + `voxel::voronoi_frand`).
- `apply_rules` internal recursion — append children to scratch list, cap at MAX_NODES.
- `plant_grow_tree` public entry: initialise root node at origin with unit tangent (0,1,0), set `base_shoot = (1,0,0)`, call `apply_rules`. After recursion, flatten via index walk.
- **Unit-shape verification:** Small test — grow one OAK_TRUNK with a fixed seed; print node count; expect ≤30.
- **Commit:** `m7.2: PlantNode scratch + apply_rules + flatten`

### T3 — Placement helper + `plant_generate_page`

- In `src/world/plant_gen.c3`: `plant_placement_for_page` hashes positions + types; `plant_generate_page` iterates, grows each tree.
- `block_heightmap_sample(block, wx, wz)` — nearest-neighbour on `block.heightmap` (CPU array filled by `generate_heightmap_cpu` in M6).
- `WATER_LEVEL_Y = 16.0f` constant.
- Need a `uint world_seed` — add a const `WORLD_SEED = 1337u` in `src/main.c3` for now (vault todo: promote to a `World` struct field in M7.4+).
- **Commit:** `m7.2: per-page tree placement`

### T4 — Hook into `geometry_generate_page`

- Rename `geometry_generate_smoke_test` → `geometry_generate_page` (from M7.2 spec).
- Inside, after voronoi + smoke boxes, call `plant_generate_page(out, page, block, WORLD_SEED, &scratch_nodes)`.
- Thread a scratch `PlantNodeList` from `main.c3::drain_dirty` (one allocation per frame, reused).
- **Commit:** `m7.2: wire plant_generate_page into drain_dirty`

### T5 — GPU `eval_tree_bezier`

- Replace magenta stub in `resources/shaders/generate.comp.glsl` with the tangent-line approximation from spec §GPU Evaluation.
- Leaf sphere material pulled from `e.thick_vals.w`.
- Update `TreeEntry` GLSL struct comment if `thick_vals.w` usage changed.
- **Commit:** `m7.2: GPU tangent-line Bezier + leaf sphere eval`

### T6 — Review + close-out

- `/requesting-code-review` via `superpowers:code-reviewer`.
- Fix Critical/Important issues.
- Send `task.completed` for `c3voxel/m7.2` to vault.
- Mark task #18 complete.

## Risk Register

| Risk | Mitigation |
|------|------------|
| Float-array in `PlantRules` doesn't initialize as a compile-time const | Check with minimal test first; fallback is to fill at runtime via a `plant_rules_init()` call |
| Too many entries/page blows SSBO budget | MAX_ENTRIES_PER_PAGE = 1024 from M7.1; 8 trees × max 340 nodes = 2720 worst case. Actually hits limit. Cap tree count at 3 for root-heavy variants OR bump MAX_ENTRIES_PER_PAGE to 4096 if observed |
| Magenta-stub trees (nearest-match wins over boxes) ruin M7.1 smoke | Remove smoke boxes from `geometry_generate_page` in T4 (replaced by trees); OR offset boxes to a different Y band so both visible during M7.2 |
| Tangent-line approx fails near sharp bends | Inspect visually; fall back to piecewise polyline if artifacts show (unlikely per VQ experience) |

## Files Touched

**New:**
- `src/voxel/plant.c3`
- `src/world/plant_gen.c3`

**Modified:**
- `src/voxel/geometry.c3` (comment about thick_vals.w = leaf_material_id)
- `src/world/geometry.c3` (rename + wire plant_generate_page)
- `src/main.c3` (scratch PlantNodeList, WORLD_SEED const)
- `resources/shaders/generate.comp.glsl` (eval_tree_bezier)
