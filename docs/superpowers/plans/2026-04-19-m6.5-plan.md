# M6.5 — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-04-19-m6.5-design.md`
**Task:** #11 (in_progress)

## Task Breakdown

Ordered per spec §Implementation Order. Each task ends with `/requesting-code-review` and a commit.

### Phase 1

#### T1 — Todo 10: UBO std140 convention

- Create `docs/ubo-convention.md` (rules + promotion path note).
- Add `$assert(T.sizeof % 16 == 0);` at module scope for each UBO:
  - `src/voxel/raymarch.c3` → `RayMarchUniforms`
  - `src/voxel/generate.c3` → `GenerateUniforms`
  - `src/render/composite.c3` → `CompositeUniforms`
  - `src/voxel/distance_field.c3` → JFA uniforms (if present)
- Add comment block above each UBO struct with promotion-to-A note.
- **Validation:** `c3c build` green; deliberately breaking one struct (e.g. inserting `Vec3`) should fail compile.
- **Commit:** `m6.5: todo-10 UBO std140 convention (docs + asserts)`

#### T2 — Todo 3: `$feature(HOT_RELOAD)`

- Edit `project.json`:
  - Add `"features": ["HOT_RELOAD"]` to `windows` and `linux` targets.
  - Add `linux-release` and `windows-release` targets with `"features": []` + `"opt": "O3"`.
- Edit `src/gpu/shader.c3::shader_reload_if_changed`:
  ```c3
  $if !$feature(HOT_RELOAD):
      return false;
  $endif
  ```
- **Validation:**
  - `c3c build` (default target) — reload still works.
  - `c3c build --target linux-release` — compiles; strings `[shader] reloaded` etc. absent from disasm.
- **Commit:** `m6.5: todo-3 gate shader hot-reload behind $feature(HOT_RELOAD)`

#### T3 — Todo 6: Hierarchical GPU timer

- New file `src/gpu/timer.c3` — `Timer`, `TimerNode`, `NodeIndex` alias, init/destroy/begin_frame/end_frame/resolve_prev/root.
- Add `time_push`/`time_pop` methods to `Commands` in `src/gpu/cmd.c3`. Commands gains a `Timer*` member; `Commands.init` takes one in.
- Wire main loop in `src/main.c3` — push `frame`/`stream`/`generate`/`composite`.
- Implement F12 keybind in `src/platform/input.c3` + handler in main: pretty-print tree to stdout.
- **Validation:**
  - Run program, F12 dumps tree; all nodes have gpu_ms > 0.
  - Exceed 256 pushes in one frame deliberately (debug temp loop) — no crash, one-shot warn.
- **Commit:** `m6.5: todo-6 hierarchical GPU timer with time_push/pop`

#### T4 — Todo 4: `TextureSlot` registry

- Add `TextureSlot[MAX_TEXTURES]` static, `texture_slots_init` (reserve slot 0 as invalid).
- Rewrite `texture2d_create/destroy`, `texture3d_create/destroy` to allocate/free slots. Return packed handle.
- Add `texture_gl_id(handle)`, `texture_format(handle)`, `texture_size(handle)` helpers.
- Update `Commands.bind_texture`/`bind_texture_3d`/`bind_image_2d`/`bind_image_3d` — resolve handle, drop `Format` param.
- Update `framebuffer_destroy` to call `texture2d_destroy` on each attachment.
- Strip `Format` args at all callsites (list per spec).
- **Validation:**
  - Golden output unchanged frame-to-frame vs T3 state.
  - Destroy + reuse a handle; verify stale handle reads return `gl_id = 0`.
- **Commit:** `m6.5: todo-4 TextureSlot registry, drop explicit Format at bind sites`

**Review gate:** `/requesting-code-review` for Phase 1 (T1–T4) collectively before Phase 2.

### Phase 2

#### T5 — Todo 12: True-iso raymarch

- Rewrite `resources/shaders/raymarch.comp.glsl` per spec §Phase 2 shader block. Per-pixel AABB iteration + `page_valid_mask`.
- Rewrite `src/voxel/raymarch.c3`:
  - Delete `View`, `dispatch_into`, old `dispatch`, `dispatch_raw`, `RayMarchContext.target`/`height_target`.
  - New `RayMarchUniforms` mirroring shader layout.
  - New `dispatch_holder(&self, cmd, page_volumes[4], df, h, pages, holder_fbo, page_valid_mask)`.
  - Update `RayMarchContext.init`/`destroy`/`resize` — no longer own intermediate target textures.
- Update `src/main.c3::drain_dirty`:
  - Iterate dirty holders (already holder-granular per D3).
  - For each holder: regen dirty pages, then one `dispatch_holder` call with 4 ring slots + mask.
- Delete any lingering references to `RayMarchContext.target` elsewhere.
- **Validation:**
  - Window shows iso parallelograms (not axis-aligned squares).
  - Pan across holder boundaries — no seams.
  - Evict a holder under load and verify partial residency renders correctly.
  - Frame time within ±20% of M6 Phase 2 baseline.
- **Commit:** `m6.5: todo-12 true-iso raymarch, one dispatch per holder`

**Review gate:** `/requesting-code-review` after T5.

### Close-out

- Update `.bridge/task-counters.json` / close vault todos 3, 4, 6, 10, 12.
- Note that todo-13 remains open, scheduled for M9.
- Update `CLAUDE.md` only if new conventions warrant (ubo-convention reference, feature flag list).
- Final `/requesting-code-review` for whole M6.5.
- Mark task #11 completed.
- Commit: `m6.5: close cleanup + iso fix milestone`

## Subagent Dispatch Plan

Each T# dispatched as its own Agent (subagent_type=general-purpose) with:
- Task prompt: self-contained, referencing spec path + this plan path + target files.
- Commit at end of each subagent's run.
- Review dispatch between Phase 1 and Phase 2, and after T5.

## Risk Register

| Risk | Mitigation |
|------|------------|
| T4 touches many callsites; silent format mismatch | Remove Format param first (compile breaks until registry stores it); registry lookup is sole source. |
| T5 iso math off-by-tilt | Reuse `game::world_xz_to_screen` inverse; unit test from spec's test criteria. |
| Timer queries never resolve (GPU stall) | Double-buffer (TIMER_FRAMES=2); resolve N-1 frame only. |
| project.json target rename breaks user's muscle memory | Keep executable name `c3voxel`; document name vs target distinction in commit. |
