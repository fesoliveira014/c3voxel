# M7.3 — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-04-19-m7.3-roads-design.md`
**Task:** #19 (in_progress)

## Task Breakdown

### T1 — Persistent CPU heightmap on Block

- `Block` gains `float[] heightmap_cpu` (length `BLOCK_HEIGHTMAP_PIXELS²`).
- `BlockPool.get_or_create` allocates `heightmap_cpu = alloc::new_array(allocator, float, ...)`.
- `BlockPool.decrement_ref` (zero-refcount) frees it alongside `heightmap` texture.
- `generate_heightmap_cpu` refactored: fills `block.heightmap_cpu[]` directly, then uploads. No more per-call allocation.
- **Commit:** `m7.3: persistent CPU heightmap on Block`

### T2 — `RoadRaster` + `Block.road_texture`

- New `src/world/road.c3`: `ROAD_BIT`, `MAZE_CELL_PX`, `CITY_SPARSITY`, `INTER_NUM_TRIES`, `INTER_MAX_GEN`, `WATER_PENALTY`, `WATER_SEA_LEVEL_NORM`.
- `RoadRaster` struct: `char[] pixels`, `bool initialized`, `Allocator allocator`. Methods `init / free / clear`.
- `Block` gains `RoadRaster road` + `gpu::Texture2D road_texture` (`R8UI`, 512×512).
- Lifecycle: allocate in `get_or_create`, destroy in `decrement_ref`.
- No generation logic yet; this lands the plumbing.
- **Commit:** `m7.3: RoadRaster + Block.road_texture`

### T3 — Sparse city + maze generator

- In `src/world/road.c3`: `block_has_city(cx, cz)`, `city_size_for_block(cx, cz)` (returns 128 today).
- `draw_segment(raster, x1, z1, x2, z2, flag_bits)` — Bresenham with bounds clipping.
- `generate_maze(raster, block, center_px, center_pz, footprint_px, seed)` — iterative stack backtracker. Uses `voronoi_hash` for neighbour ordering; `block.heightmap_cpu` for Δh tiebreaker. 16×16 grid over `[192, 320)²`.
- **Commit:** `m7.3: sparse city + intra-city maze`

### T4 — Inter-city midpoint displacement

- `weigh_path(block, x1, z1, x2, z2)` — iterate Bresenham pixels, sum `|Δh|` + `WATER_PENALTY` for below-sea pixels.
- `best_path_draw(raster, block, x1, z1, x2, z2, gen, counter, seed)` — recursive greedy midpoint, draws segment on base case.
- `generate_inter_city_roads(raster, block, seed)` — for each resident city, run `best_path_draw` to its N and E neighbour city centres (clipped at block boundary).
- **Commit:** `m7.3: inter-city midpoint displacement`

### T5 — `generate_roads_for_block` entrypoint

- Public API: `generate_roads_for_block(Allocator scratch, Block* block, uint world_seed)`.
  1. `raster.clear()`.
  2. If `block_has_city(cx, cz)`: `generate_maze(...)`.
  3. `generate_inter_city_roads(...)`.
  4. `gpu::texture2d_upload(block.road_texture, raster.pixels, gpu::Format.R8UI, 0, 0, 0, 512, 512)`.
- Hook into `src/main.c3::drain_dirty`, immediately after `generate_heightmap_cpu`.
- **Commit:** `m7.3: generate_roads_for_block + drain_dirty wiring`

### T6 — Shader road sampling

- Add `layout(binding = 7) uniform usampler2D road_raster;` to `resources/shaders/generate.comp.glsl`.
- Inside `main()`, after terrain_color and before entry loops: sample road_raster at `hm_uv`, if `ROAD_BIT` set AND `floor(world.y) == floor(h_w)` → `result = material_color(2)` (DIRT).
- `GenerateContext.dispatch_page` binds `block.road_texture` at binding 7.
- **Commit:** `m7.3: shader road sampling + DIRT override`

### T7 — Review + close-out

- Subagent code review (`superpowers:code-reviewer`).
- Fix Critical/Important.
- Send `task.completed` to vault for `c3voxel/m7.3`.
- Mark task #19 complete.

## Risk Register

| Risk | Mitigation |
|------|------------|
| `char[]` byte semantics in C3 vs `ubyte` | Verify early; switch to `ichar[]` or `uchar[]` if there's a signed/unsigned issue |
| R8UI upload format mismatch | `format_channel` (`src/gpu/resource.c3`) maps R8UI to `GL_RED_INTEGER` + `GL_UNSIGNED_BYTE` — should match the raster bytes |
| Bresenham endpoint coverage | Test visually in T6 |
| Maze stack overflow at MAX_NODES ≡ 256 cells | 16×16 = 256 cells — `int[256]` stack worst case, fits easily |
| Inter-city recursion blows CPU time on block creation | 1024 segments × short weigh_path = milliseconds; single-shot per block |

## Files Touched

**New:** `src/world/road.c3`

**Modified:**
- `src/world/block.c3` (heightmap_cpu, road, road_texture, lifecycle)
- `src/world/terrain.c3` (`generate_heightmap_cpu` refactor)
- `src/voxel/generate.c3` (`GenerateContext.dispatch_page` binds road_texture)
- `resources/shaders/generate.comp.glsl` (road sampling)
- `src/main.c3` (`drain_dirty` calls `generate_roads_for_block`)
