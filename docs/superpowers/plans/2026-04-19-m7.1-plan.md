# M7.1 — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-04-19-m7.1-paramarr-materials-design.md`
**Task:** #17 (in_progress)

## Task Breakdown

Ordered per spec §Implementation Order. Each task ends with a build + runtime smoke + commit; review dispatched at the end.

### T1 — Material enum + debug palette

- New `src/voxel/material.c3`: `Material` enum (17 IDs), `MATERIAL_COUNT`, `MATERIAL_DEBUG_PALETTE: core::Vec4[MATERIAL_COUNT]`.
- No callsites yet. Pure addition.
- **Build:** `c3c build linux` green.
- **Commit:** `m7.1: todo-X material enum + debug palette`.

### T2 — `MaterialParams` + `GeomEntry` / `TreeEntry` / `LineEntry`

- New `src/voxel/geometry.c3`: `MaterialParams` (16 B), `GeomEntry` / `TreeEntry` / `LineEntry` with explicit reserved-field comments.
- `$assert(T.sizeof % 16 == 0)` at module scope for each.
- No callsites yet.
- **Build:** green; `$assert`s pass.
- **Commit:** `m7.1: geometry entry structs`.

### T3 — Voronoi seeds (CPU + UBO)

- New `src/voxel/voronoi.c3`: `VoronoiParams` struct (16-aligned, `Vec4[27]`), `voronoi_for_page(page)`, internal hash + frand helpers.
- Deterministic: `voronoi_hash((uint)page.px, 0, (uint)page.pz, cell_idx)` then perturb grid cell centers by ±0.4 of cell extent.
- `$assert(VoronoiParams.sizeof % 16 == 0)` (432 B).
- **Tests:** inline runtime smoke — generate seeds for a known coord twice, verify identical output (determinism).
- **Commit:** `m7.1: voronoi seed generator (27 seeds/page)`.

### T4 — `PageGeometry` + smoke generator

- New `src/world/geometry.c3`: `PageGeometry` struct with `List{GeomEntry}`, `List{TreeEntry}`, `List{LineEntry}`, inline `VoronoiParams`.
- Methods: `init(allocator)`, `free()`, `clear()`.
- `geometry_generate_smoke_test(&PageGeometry, Page*)`: 9 `GeomEntry`s on a 3×3 grid at the page surface, materials cycle `STONE, BRICK, PLASTER, WOOD, SHINGLE, MORTAR, DIRT, GRASS, SAND`; reserved fields zeroed.
- Wire into `Page`: add `PageGeometry geometry` field. `PagePool.get_or_create` calls `init`; `PagePool.destroy` calls `free`; slot-recycle path calls `clear`.
- **Build:** green; `geometry` field accessible on `Page`.
- **Commit:** `m7.1: PageGeometry + 3x3 smoke-test generator`.

### T5 — `GenerateContext` SSBO + UBO plumbing

- `GenerateUniforms`: add trailing `IVec4 entry_counts`. Re-verify `$assert`.
- `GenerateContext` gains `gpu::Buffer geom_ssbo`, `tree_ssbo`, `line_ssbo`, `voronoi_ubo`. Sized `MAX_ENTRIES_PER_PAGE * 160 B` for each SSBO (= 160 KB); `VoronoiParams.sizeof` for the UBO.
- `init` creates them with `buffer_create(..., DYNAMIC)`; `destroy` releases.
- New method: `GenerateContext.dispatch_page` extends to accept `PageGeometry*` — uploads geom/tree/line/voronoi to the 3 SSBOs + UBO, sets `entry_counts` on the uniforms, then dispatches.
- Temporary until T6: shader still uses old logic; entry_counts are ignored.
- **Build:** green. Runtime: unchanged behavior.
- **Commit:** `m7.1: GenerateContext SSBO + voronoi UBO plumbing`.

### T6 — `generate.comp.glsl` rewrite + drain_dirty wiring

- Rewrite `resources/shaders/generate.comp.glsl`:
  - Bindings: `volume`(0), `heightmap`(1), UBO(2), `GeomEntries`(3), `TreeEntries`(4), `LineEntries`(5), `VoronoiU`(6).
  - Terrain base color (existing band logic) as fallback.
  - Three nearest-match loops against `entry_counts.xyz`, shared `bestDis/nextBestDis/bestIdx/bestType` tracker.
  - `eval_geom_superellipsoid` per doc §6.5 and spec §GPU Pipeline.
  - Stubs for `eval_tree_bezier` / `eval_line` (write magenta so misroutes are visible).
  - `voronoi_lookup_grad` returning `1.0 - 2*d1/(d1+d2)`; applied as subtle brightness.
- `src/main.c3::drain_dirty`: for each page, call `geometry_generate_smoke_test(&page.geometry, page)`, then pass `&page.geometry` into `gen.dispatch_page`.
- **Build:** green. Runtime: visible 3×3 grid of superellipsoids per page; verify against spec §Testing checklist.
- **Commit:** `m7.1: generate.comp rewrite + paramArr dispatch`.

### T7 — Code review + close-out

- `/requesting-code-review` via `superpowers:code-reviewer` subagent.
- Fix any Critical/Important issues; Minor as follow-ups.
- Send `task.completed` to vault for `c3voxel/todo-17` (M7.1 milestone).
- Mark task #17 complete.

## Subagent Dispatch Plan

- T1–T5 are small enough to run sequentially in-session (each under ~80 LOC).
- T6 is the big one (shader rewrite + main-loop plumbing); run in-session, not delegated — the runtime verification needs the current session's context.
- Code review (T7) via subagent.

## Risk Register

| Risk | Mitigation |
|------|------------|
| SSBO upload stalls at 9 entries/page × 16 holders × N frames | Entry counts are trivially small (< 1 KB per page). Stalls would only appear with 1000s of entries (M7.4 territory). |
| Voronoi determinism across regenerations broken by hash | Unit-test by calling `voronoi_for_page` twice for the same page coord and asserting memcmp equal. |
| Nearest-match "shared tracker" produces wrong colors at boundaries | Visual: check in smoke output that adjacent superellipsoid boundaries have clean material transitions; if wrong, suspect the `vis_min/vis_max` cull or shared tracker update order. |
| Terrain band fallback writes over a valid entry | Check flow: only write terrain when `best_type < 0`. Assert in review. |
| `Page.geometry` lifecycle leaks across page-slot recycle | Explicit test: evict a page whose geometry holds data, verify `clear()` was called and lists are length 0 + capacity retained. |

## Files Touched Summary

**New:**
- `src/voxel/material.c3`
- `src/voxel/geometry.c3`
- `src/voxel/voronoi.c3`
- `src/world/geometry.c3`
- `docs/superpowers/specs/2026-04-19-m7.1-paramarr-materials-design.md` (already committed)

**Modified:**
- `src/voxel/generate.c3`
- `src/world/page.c3`
- `src/main.c3`
- `resources/shaders/generate.comp.glsl`
- `docs/ubo-convention.md` (minor: "5th UBO" → "6th UBO")
