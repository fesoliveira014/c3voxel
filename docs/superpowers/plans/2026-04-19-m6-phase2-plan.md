# Implementation Plan — M6 Phase 2

Spec: `docs/superpowers/specs/2026-04-19-m6-phase2-design.md` (rev @ 7d79e54).

Execution: six sequential tasks. Each keeps `c3c build linux` green,
runs a smoke at milestone boundaries, commits with a prescribed
message.

Dependencies: Phase 1 landed (commits 21de210 → 8705a24). Current
runtime is a static 4×4 top-down grid.

---

## Task 1 — Core primitives + platform input

**Files:**
- MODIFY `src/world/coords.c3` — add `IVec3` re-export (already present via core::IVec3; no change unless missing).
- MODIFY `src/world/block.c3` — add `int holder_ref_count` to Block; `BlockSlot` gains the field; `BlockPool.get_or_create` increments on first call and on reuse; add `BlockPool.decrement_ref(BlockId)` that decrements and destroys heightmap + bumps generation at zero.
- MODIFY `src/world/holder.c3` — add `fn void HolderPool.evict(&self, HolderId)` that bumps `slot.generation`, calls `gpu::framebuffer_destroy`, removes from `lookup`, sets `alive = false`, calls `BlockPool.decrement_ref(block_of(holder))`. Adds `fn BlockId HolderPool.block_of(Holder*)` helper (or stash `BlockId` on `Holder`).
- MODIFY `src/platform/window.c3` — register GLFW mouse cursor, button, scroll callbacks; add a `MouseState` field on `Window`; callbacks write into `win.mouse` via the existing user pointer.
- NEW `src/platform/input.c3` — `struct MouseState { double x, y, dx, dy; bool left_down; int scroll_steps; }` + `fn void input::poll(MouseState* out, Window* win)` that snapshots + clears accumulators.

**Build gate:** `c3c build linux` clean. No behavioural change visible at runtime.

**Commit:** `M6 P2 T1: platform input + block/holder evict + refcount`

---

## Task 2 — GpuPool + DirtyQueue + visibility

**Files:**
- NEW `src/world/streaming.c3` — `struct PoolSlot`, `struct GpuPool { PoolSlot[POOL_SIZE]; uint current_frame; }`, `alias DirtyQueue = List{HolderId};`, plus methods `GpuPool.init/acquire/touch/find_slot`.
- NEW `src/world/visibility.c3` — `struct HolderRect { HolderCoord x0, z0, x1, z1; }` and `fn HolderRect visible_holder_rect(CameraState* cam)` that derives the world-aligned XZ rectangle from the iso view + `VISIBLE_MARGIN_HOLDERS`.
- Inside `streaming.c3` — `fn void streaming::update(pool, blocks, holders, pages, dirty, cam)` per spec §5; `fn void streaming::drain_dirty(cmd, gen, jfa, rm, ring, df, blocks, holders, pages, pool, dirty, cam, budget)` per spec §6 + 2-addendum.4 starvation policy.

**Build gate:** `c3c build linux` clean. Not yet wired into `main`; new modules sit unused.

**Commit:** `M6 P2 T2: streaming — GpuPool + DirtyQueue + visibility`

---

## Task 3 — Iso raymarch + UBO

**Files:**
- MODIFY `src/voxel/raymarch.c3` — `RayMarchUniforms` replaced per spec 2-addendum.2 (iso_forward, pan_target, holder + page bounds, sprite_corner, tilt, zoom). New `fn void RayMarchContext.dispatch_holder_page(cmd, ring, slot, df, page, holder_world_aabb, holder_fbo, sprite_corner, cam)` replaces `dispatch_into`. Old top-down `dispatch_into` retired; single-volume `dispatch` kept for existing smoke paths that don't care.
- MODIFY `resources/shaders/raymarch.comp.glsl` — ray origin per pixel computed via the inverse iso map (spec 2-addendum.1). MRT color + height writes unchanged shape; height stored is the hit's **iso screen Y** (not world Y) so the composite's max-height compare resolves occlusion correctly.
- MODIFY `src/game/camera.c3` — `CameraState` gains `Vec3 pan_target` + `float zoom_scale`; `camera_isometric(pan_target, zoom_scale, aspect, azimuth_deg)` replaces the Phase 1 two-arg factory. Phase 1 call sites in `main.c3` stay broken until Task 5 rewires them.

**Build gate:** `c3c build linux` clean. Expect `main.c3` compile break; Task 4-5 fix it. **Accept the break** — keep the commit compiling the library but flag main as transiently broken in the commit message.

Correction: build gate should be green. Implementer should stub main.c3's `rm.dispatch_into` calls with placeholder so build passes, then Task 5 deletes them.

**Commit:** `M6 P2 T3: iso raymarch — UBO, inverse map, iso-space height MRT`

---

## Task 4 — Composite 16-slot expansion

**Files:**
- MODIFY `src/render/composite.c3` — `CompositeUniforms` expanded per spec 2-addendum.3 (16 × vec4 screen_min/max; ivec4 alive_bits bitmask). `CompositeContext.dispatch` now takes a `GpuPool*` + `HolderPool*` + `CameraState*` and derives per-slot screen rects from the holder's world AABB via the iso forward map.
- MODIFY `resources/shaders/composite.frag.glsl` — 16 sampler pairs at bindings 0..31; UBO at binding 32; alive_bits bitmask loop.

**Build gate:** `c3c build linux` clean. Smoke: standalone test that binds 16 dummy single-colour sprites, dispatches composite, observes a known pattern (e.g. slot 0 red at (0,0), slot 15 green at (640,640)).

Note: 16 sampler pairs exceeds typical c3c 0.7.11 shader reflection budget. If SPIR-V loader or glSpecializeShader complains, fall back to one `sampler2DArray` per layer (color array + height array). Keep the C3-side binding API stable.

**Commit:** `M6 P2 T4: composite — 16-slot iso rects + alive-bits bitmask`

---

## Task 5 — Main rewire: streaming pipeline

**Files:**
- REWORK `src/main.c3`:
  - Drop the Phase 1 startup loop entirely (no pre-generated 4×4 grid).
  - Create `GpuPool pool; pool.init();`, `DirtyQueue dirty;`, the three pools (block/holder/page), shared `VolumeRing`, `DistanceField`, `GenerateContext`, `JumpFloodContext`, `RayMarchContext`, `CompositeContext`.
  - Per-frame: `input::poll → apply_pan_zoom → streaming::update → streaming::drain_dirty → composite::dispatch`.
  - `apply_pan_zoom(&cam, &mouse)`: left-drag updates `cam.pan_target.xz` with `PAN_SENSITIVITY * cam.zoom_scale`; scroll clamped into { 0.5, 1.0, 2.0 } steps; on zoom change, invalidate all pool slots (mark dirty).
  - Retain hot-reload cycle across all 7 shader programs.
  - Retain the 60-frame FPS print.

**Build gate:** `c3c build linux` clean. Runtime smoke: launch → terrain fades in within ~1 s → drag mouse → holders appear/evict → scroll wheel → zoom steps through 0.5×/1×/2×.

**Commit:** `M6 P2 T5: main — streaming pipeline, drop Phase 1 static loop`

---

## Task 6 — Acceptance + todo sweep

**Files:** None (verification + bridge).

**Steps:**
1. Manual smoke per spec §13 / 2-addendum.5 acceptance criteria (user-side). Document results in the commit message.
2. Close `todo-7` if Phase 2 threaded `WorldPixel`/`Meter` etc through the pool APIs.
3. File any new deferred items as `todo-N`:
   - GPU heightmap generation (still todo-2).
   - Bindless composite (still M9 territory).
   - Possibly: `sampler2DArray` fallback if Task 4 required it.
4. Mark task #6 `completed` in the harness + emit `task.completed` to vault.

**Commit:** `M6 P2 T6: acceptance sweep, close M6 Phase 2`

---

## Execution rules

- Sequential: no task starts before the previous commit lands with green build.
- Each task's commit message follows the `M6 P2 Tn: ...` prefix.
- `c3c build linux` green is load-bearing between tasks.
- Smoke-run only at task boundaries where a visible runtime change is
  possible (T4, T5, T6).
- If a task's fallback path fires (e.g. T4's sampler array), file a
  todo explaining the deviation so reviewers have breadcrumbs.
